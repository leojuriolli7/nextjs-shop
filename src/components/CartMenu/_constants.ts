import * as animationData from "@assets/animations/cart.json";

export const LOTTIE_CONFIG = {
  loop: true,
  autoplay: true,
  animationData: animationData,
  rendererSettings: {
    preserveAspectRatio: "xMidYMid slice",
  },
};
